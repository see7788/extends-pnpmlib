# extends-pnpmlib

`see7788/extends-pnpmlib` 是个人 TypeScript 源码包模板。源码直接放入 `src` 并作为包内容使用，不要求预编译到 `dist`。

```powershell
pnpm install
pnpm typecheck
```

创建实际库后，替换本说明和 `src/index.ts` 中的占位内容，并按需要添加 dependencies、peerDependencies 与对应开发依赖。
